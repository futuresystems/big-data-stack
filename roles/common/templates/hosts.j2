#jinja2:lstrip_blocks: True
#{{ ansible_managed }}

127.0.0.1 localhost localhost.{{ domain_name }}
::1 localhost localhost.{{ domain_name }}

{% for host in groups['all'] %}
{{ hostvars[host]['ansible_' + iface_internal].ipv4.address }} {{ hostvars[host].inventory_hostname }} {{ hostvars[host].inventory_hostname }}.{{ domain_name }}
{% endfor %}
